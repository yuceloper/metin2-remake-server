# Evidence status: reference-confirmed
# Reference: yuceloper/new-metin src/Executables/Game/Packets/CharacterPoints.cs and PlayerEntity.cs
# Point semantics are indexed by the legacy EPoints table; most entries are zero/derived at runtime.
# The packet is used during initial Loading bootstrap and later during Game updates.
# Final compatibility must still be verified against the exact target client/capture.
schema: 1
protocol: legacy-metin2
packets:
  - name: CharacterPoints
    opcode: 0x10
    direction: server_to_client
    phase: any
    size: fixed
    sequence: false
    since: 1
    fields:
      - name: points
        type: array
        length: 255
        element:
          type: u32le
